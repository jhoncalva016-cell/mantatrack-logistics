{
  "name": "mantatrack-logistics",
  "private": true,
  "version": "1.0.0",
  "engines": {
     "node": "22.x"
   },
  "scripts": {
    "build": "cd client && npm install && VITE_API_URL= npm run build && cd ../server && npm install",
    "start": "npm start --prefix server"
  }
}
